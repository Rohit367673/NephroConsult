# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: email.spec.ts >> Email Confirmation >> should show payment success page
- Location: e2e/email.spec.ts:4:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Payment Successful')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('text=Payment Successful')

```

```yaml
- img
- heading "Payment Failed" [level=2]
- paragraph: Order ID not found in URL.
- button "Try Again"
- button:
  - img
- region "Notifications alt+T"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Email Confirmation', () => {
  4  |   test('should show payment success page', async ({ page }) => {
  5  |     await page.goto('/payment/success');
> 6  |     await expect(page.locator('text=Payment Successful')).toBeVisible();
     |                                                           ^ Error: expect(locator).toBeVisible() failed
  7  |   });
  8  | 
  9  |   test('should display confirmation message', async ({ page }) => {
  10 |     await page.goto('/payment/success');
  11 |     await expect(page.locator('text=Confirmation')).toBeVisible();
  12 |   });
  13 | 
  14 |   test('should show appointment details on success', async ({ page }) => {
  15 |     await page.goto('/payment/success');
  16 |     await expect(page.locator('text=Appointment')).toBeVisible();
  17 |   });
  18 | });
  19 | 
```